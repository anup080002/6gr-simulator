# 5 MHz / 12 dB diagnostic: failed acceptance

## Execution and source

The unchanged TDD diagnostic began on 17 September at approximately 06:31
IST, using frozen commit `68140bb94be8501f4c07faf5038746c1ae543112` and
`lls_causal_access_to_data_wiring_tdd_short_continuous_iq.yaml` without
scenario overrides. All 58 scheduler slots and the receive tail executed.
The raw trial tables contain **20/20 DL and 5/5 UL CRC-passing attempts**.
This does not constitute integrated scenario acceptance.

The 4,636-file source SHA256 was unchanged before execution, after PHY
execution and during recovery:
`FAD898EC5F9C971716FEC0709D084AB7E4933A8A2CEF77FC8FC5F5B00E392560`.
Newer commits preserved in the work branch were not used by this run.

## Required failures before report recovery

The pre-recovery manifest records `ResultOk=false`,
`RunCompletion=completed_with_failures` and these four required failures:

1. `MIMO_NominalEffectiveEvidence`: the UL runtime antenna domain does not
   match the configured execution contract.
2. `csi_feedback_cqi_ri_pmi_cri`: `CALLED_NO_MEASUREMENT`.
3. `beam_precoder_application`: `BLOCKED_BY_DEPENDENCY`.
4. `cqi_inner_loop_link_adaptation`: `BLOCKED_BY_DEPENDENCY`.

The original manifest, case-status table, MIMO gate and sealed IQ manifest
are preserved in `evidence_20260917/tdd_12db_pre_recovery` beside this file.
At that stage the runtime truth-contract check passed; that separate check
did not override the four required failures.

At 09:11 IST, report finalization raised
`sixgr:artifact:TerminalBrowserClosureFailed` after three attempts.
Earlier reporting diagnostics identified FER frame-population disagreement
and missing valid samples in the requested constellation plane. No receiver
plane was substituted and no acceptance threshold was weakened.

## Terminal result

MATLAB exited **1**, confirmed at 10:02 IST. The final exception is
`sixgr:artifact:TerminalBrowserClosureFailed`. Automatic recovery also
failed with `sixgr:artifact:BrowserMaterializationFailed` on FER frame-count
population reconciliation (rows 1 and 2). Recovery labels the manifest `failed`
with `runtime_incomplete_recovered_from_persisted_raw_evidence`. Its changed
failure count is a recovery-status reduction, not evidence that any original
failure was repaired. Its strict truth failure count is 2, while the
pre-recovery truth verdict was a separate earlier pass. The original large
MAT result and raw IQ remain in the frozen worktree. Further bug repairs
were stopped as requested.

Final console-log SHA256:
`261780FD383571786BD8CF05BF32FA9E24A24386679EE6ED86B640EAECDBB0BE`.
`terminal_error.mat` and `terminal_receipt.json` are retained beside the log.

Local console log and source receipts:
`logs/tdd_5mhz_12db_68140bb9_20260917`.
`artifact_location.json` there identifies the full original artifact tree.
The complete terminal tree is also copied to its `artifacts` subfolder:
1,392 files, 3,961,101,003 bytes, with zero SHA256 mismatches and originals
retained. The final frozen-source checksum still matches the starting value.

The work branch is a preserved, unqualified checkpoint, not qualified main.
The never-started `5d636954` regression queue was retired when new custom
research-carrier implementation began; its receipts are preserved under
`logs/checkpoint_5d636954_testall_20260917`. It is not a test pass. One full
suite on the updated source is required after focused validation.
No full-suite pass, R2023b qualification or 400 MHz link pass is claimed.
