# LLS CODEX Issue Ledger

Last updated: 2026-09-12

This ledger tracks the CODEX-01..40 repair stream against concrete repo files and focused tests. A row is only marked fixed when the implementation path and a focused validation are both present. Rows marked partial or pending must not be described as 3GPP-complete in reports, CSV manifests, DB rows, or WebGUI badges.

## Current execution gate and ordered roadmap

Latest checkpoint (2026-09-12): the `_02` spatial re-audit and approved
two-port SRS/PUSCH work are tracked in
[the spatial repair ledger](lls/continuous_iq_02_spatial_reaudit_20260912.md).
The corrected TPMI and [SRI/DM-RS signaling](lls/ul_reference_signaling_repair_20260912.md)
shared-clock regressions passed. The next full
58-slot run remains held for complete configuration-owned control context and receiver payload-size authority;
do not interpret earlier functional-run rows as closure of these new checks.
The `continuous_iq_03` attempt was stopped during initialization and is not
a new PHY result. All original outputs remain local and unchanged.

The 58-slot 12 dB configured-SNR TDD run is the only immediate completion
target. It is an actual shared-waveform LLS diagnostic, not an E2E campaign and
not statistical publication evidence.

| Order | Scope | Status / acceptance |
|---|---|---|
| 1 | Operator-master PDSCH/PUSCH TDRA catalogs | Completed in commit `a3dc4b9f`; focused operator-master contract passed. |
| 2 | Shared-clock 12 dB PHY execution | **Functional pass.** Run `tdd_12db_continuous_iq_01` executed all 58 slots without the former slot-44 crash: four-step RA succeeded, 12 PDSCH and 5 PUSCH trials completed with CRC pass, and the active runtime exported PBCH, PRACH, PUCCH, SRS, TRS, CSI/PMI/RI, PDCCH grant binding, AMC and exact continuous post-IFFT/post-TX-RF/pre-channel I/Q. DL AMC causally moved from bootstrap MCS 1/QPSK to MCS 20-22/64-QAM. Median receiver post-equalization SINR was 19.909 dB DL and 12.216 dB UL at the declared configured 12 dB reference point. |
| 3 | Functional artifact closure | **Functional pass; production-publication fail.** The continuous run materialized 738 browser artifacts with zero missing tables/charts and finished with `ResultOk=1`, runtime truth PASS, standards PASS, mandatory subsystems PASS, active issue gate PASS, visual/duplicate gates PASS, and zero primary proxy/fallback evidence. Its official finalization terminal is nevertheless `FAIL`/`PublicationQualified=0`: Phase-7 numerical validation, long-run/statistical evidence and independent FRC qualification are not satisfied. The run must be described as `FUNCTIONAL_ONLY_NOT_PRODUCTION_QUALIFIED`, never as fully passing publication qualification. |
| 4 | Full repository qualification and production publication | **Functional gate repairs committed; fresh run pending.** Commits `b869b1c5` and `dc1d9fb0` separate RF timing impairment from the true shared-clock receiver arrival, qualify the deliberately unavailable post-RF scalar noise variance only through strict reference-derived downstream evidence, and explicitly mark campaign/statistical/long-run gates N/A for bounded diagnostic run classes without changing their false evidence values. Focused DL shared-CDL, UL SRS/DCI/PUSCH/UCI, noise-domain, reconciliation and Phase-7 reduction tests pass. The historical run remains immutable and retains its old terminal failure; one fresh 58-slot execution is required. Production publication remains a separate pending campaign requiring checkpoint/seed/multi-seed/CI/sample/sweep/determinism/long-run and applicable independent-reference evidence. `testAll` remains explicitly stopped. One manual AWGN DL fixture also retains an outdated finite-CFO assertion when disabled CFO is correctly N/A. |
| 5 | Keysight playback package | **Software package pass; physical loopback pending.** Run `tdd_12db_continuous_iq_01` produced a gap-free 58-slot capture for two gNB and two UE physical ports (445,440 samples/port), and `tdd_12db_continuous_iq_01_keysight_01` passed source-hash, common-clock, common-endpoint-scaling, WIQ readback and 89600 MAT readback checks with zero clipped components. Physical M9384B/M9383B/89600 recall, synchronization and analyzer decode still require the instruments/operator and are not claimed. |
| 6 | Single-carrier 7 GHz/400 MHz | **Starts after one fresh 12 dB functional run confirms the repaired timing/noise evidence.** No additional repeated baseline run is planned. This remains an `optional_research_experiment`, never four 100 MHz carriers. Required gates include custom FR3 opt-in, numerology/grid/FFT/sample-rate consistency, PRACH/SSB/control/BWP legality, memory-safe shared CDL execution and Keysight playback. |
| 7 | UL 1024-QAM and DL 4096-QAM | Pending fail-closed experimental integration through scheduler, control fields, TBS, modulation, receiver and AMC. DL 4096-QAM is not an NR conformance claim, and a 30 dB run alone is not proof of reliable 4096-QAM. |
| 8 | 30 dB run | Pending after steps 5-7. |
| 9 | Impairment and physical-link-budget variants | Pending separate profiles. The fixed-SNR baseline keeps ideal RF; O2I/path loss/shadowing and hardware stress are not silently mixed into it. |

The exact operator commands and configuration rules are maintained in
`docs/LLS_RUNBOOK.md`.

The `_03` failed-run cleanup repeatedly regenerated the full coverage,
component-view and strict materialization artifacts after the terminal receipt
was already sealed. The successful `_04` run also spent most of its 11,427 s
elapsed time in repeated strict raster materialization/final auditing after the
58-slot waveform bundle had completed. De-duplicating that reporting-only path
is a performance follow-up; it must not weaken the truth, visual, or publication
gates.

| ID | Issue | Current status | Root-cause evidence | Owner files / validation |
|---|---|---|---|---|
| CODEX-01 | DL LLR / noise variance units mismatch | Fixed before this pass, re-audited | `PDSCH_Rx` now computes `nVarDecode` from post-equalization SINR/CSI and passes it to `nrPDSCHDecode`; pre/post noise are exported separately. | `+sixgr/+phy/+dl/PDSCH_Rx.m`, `+sixgr/+phy/+rx/postEqualizationNoiseVariance.m`, `tests/testPostEqSINR.m` |
| CODEX-02 | UL LLR / noise variance units mismatch | Fixed before this pass, re-audited | `PUSCH_Rx` mirrors DL post-equalization noise handling and stores decoder-domain `NoiseVar`. | `+sixgr/+phy/+ul/PUSCH_Rx.m`, `tests/testULNoiseVarianceValidation.m` |
| CODEX-03 | EVM computation units mismatch | Mostly fixed / focused pass | EVM-derived SINR remains quarantined away from decoder-truth fields, and the focused receiver guard now verifies EVM as RMS error against a unit-power constellation with mathematically traceable diagnostic SINR metadata only. | `+sixgr/+link/deriveModulationTrackingMetrics.m`, `+sixgr/+link/deriveDecoderTruthProxySINR.m`; `testPostEqSINR`; pending waveform-standard EVM mask calibration by modulation |
| CODEX-04 | HARQ IR soft combining not wired | Mostly fixed / verified | Coupled-truth HARQ roundtrip now exercises real retransmission evidence in focused validation. | `testLLSCoupledTruthHARQRoundTrip`; pending larger HARQ LLR gain distribution audit |
| CODEX-05 | Scheduler SINR source uses large-scale instead of measured evidence | Mostly fixed / focused pass | Runtime paths preserve receiver-Hest vs system-level SINR separation, closed-loop AMC passes, grant plans/trial CSV/canonical exports disclose bootstrap vs measured CQI, and strict measured-only mode now blocks new-data grants until runtime CQI evidence exists instead of fabricating a measured MCS. Full scheduler-priority audit remains for large WebGUI runs. | `SystemLevelRunner.m`, `SchedulerBase.m`, `runDLPDSCHThroughput.m`, `runULPUSCHThroughput.m`, `exportSystemLevelCanonicalArtifacts.m`; `testSchedulerAMCNoFixedMCSFallback`, `testLLSClosedLoopLinkAdaptation`, `testSystemLevelSourceLabelSeparation`, `testSystemLevelWaveformReplayEvidence`; pending scheduler trace audit |
| CODEX-06 | OLLA not applied | Mostly fixed / focused pass | Focused closed-loop link adaptation validation now passes, showing MCS increases after good CSI. | `testLLSClosedLoopLinkAdaptation`; pending long-run ACK/NACK convergence telemetry |
| CODEX-07 | CQI-to-SINR mapping not calibrated | Mostly fixed / focused pass | MCS resolution uses CQI/MCS tables instead of approximate modulation mapping, and focused validation now checks non-uniform table1/table2 SINR-threshold behavior plus threshold lineage. Sweep-backed BLER calibration of the lab-default thresholds remains a separate calibration task. | `+sixgr/+link/resolveMCSIndexFromProfile.m`, `+sixgr/+link/resolveWidebandCQI.m`; `testNRCQITableAndGrantSizing`; pending controlled-sweep threshold calibration |
| CODEX-08 | Bootstrap CQI/MCS warmup too conservative | Mostly fixed / focused pass | Conservative bootstrap remains available and clearly labeled for scenarios that request it, and new `bootstrap_cqi_mode=require_measured_cqi` / `measured_only` blocks new-data grants until runtime CQI feedback exists. Scheduler decisions, grant plans, raw trial CSVs, and canonical exports preserve `bootstrap_cqi_conservative` vs `cqi_required_no_runtime_feedback` vs measured `cqi_table` provenance. | `+sixgr/+l2/+mac/SchedulerBase.m`, `+sixgr/+lls6g/buildInternalConfig.m`, `simulator/configs/schema/scenario_parameter_catalog_extension_03.yaml`, `+sixgr/+truth/exportSystemLevelCanonicalArtifacts.m`; `testSchedulerAMCNoFixedMCSFallback`, `test6GScenarioConfigValidation`; pending measured-first WebGUI audit |
| CODEX-09 | LDPC decoder iteration limit insufficient | Mostly fixed / focused pass | Runtime LDPC iteration resolution now uses a conformance-style floor of 50 iterations, raises to at least 200 for ultra-reliable BLER targets, and raises the floor for concrete CDL/TDL fading channels instead of silently keeping an 8-iteration lab default. | `+sixgr/+phy/+phycode/resolveLDPCMaxIterations.m`; `testLLSExtendedPhysicalGuards`; pending decoder stress/BLER-vs-iteration sweep |
| CODEX-10 | PRACH AppliedAWGNSNR calibration error | Focused pass | PRACH LLS and control coverage tests pass; no current evidence of the old format/calibration crash. | `testPRACHLLS`, `testTruthValidationControlCoverage`; pending large-run detection-rate audit |
| CODEX-11 | CQI smoothing alpha fixed vs Doppler | Mostly fixed / focused pass | CQI smoothing remains explicitly fixed when YAML/config provides `cqi_smoothing_alpha`, and now also supports `cqi_smoothing_mode=doppler_adaptive`, deriving alpha from Doppler/coherence time and slot duration with provenance in `CQISmoothingAlphaSource`. Focused tests verify fixed-alpha provenance and higher-Doppler faster smoothing. | `+sixgr/+link/computeLinkAdaptationDecision.m`, `+sixgr/+lls6g/buildInternalConfig.m`, `simulator/configs/schema/scenario_parameter_catalog_extension_03.yaml`; `testLinkAdaptationDomainTruth`, `testLLSStatefulLinkAdaptationState`, `testLLSLinkAdaptationRadisysMCSLoop`, `test6GScenarioConfigValidation`; pending live CQI threshold/calibration validation |
| CODEX-12 | EESM beta not calibrated per MCS | Mostly fixed / focused pass | Effective-SINR CQI now supports an explicit EESM beta catalog keyed by MCS index, exported with `EffectiveSINRBetaSource` and `EffectiveSINRBetaValueRole`. Scalar beta remains supported but is labeled as configured scalar or lab default, not per-MCS calibration. | `+sixgr/+link/resolveWidebandCQI.m`, `+sixgr/+lls6g/buildInternalConfig.m`, `simulator/configs/schema/scenario_parameter_catalog_extension_06.yaml`; `testLLSEffectiveSINRBLERLUTSelection`, `testLinkAdaptationDomainTruth`, `test6GScenarioConfigValidation`; pending sweep-backed beta calibration values |
| CODEX-13 | HARQ K1/K2 timing hardcoded | Mostly fixed / focused pass | Root cause was config bridging: `buildInternalConfig` mapped `harq.feedback_timing_slots` to `cfg.phy.harq.feedbackTimingSlots`, while PF/RR schedulers consumed `cfg.mac.harq.k1/k2` and could retain defaults. The bridge now writes scheduler K1 plus UL K2 aliases, and the focused HARQ test proves changed scenario timing reaches actual PF scheduler grants. | `+sixgr/+lls6g/buildInternalConfig.m`, `tests/testLLSHARQExercise.m`; `testLLSHARQExercise`, `testSchedulerGrantConsistency`; pending full TDD feedback/PUCCH timing audit |
| CODEX-14 | Phase noise model not Lorentzian | Mostly fixed / focused pass | Configured phase noise is now materialized in the sample-domain waveform path using either `comm.PhaseNoise` or a runtime colored PSD mask derived from carrier frequency; focused validation confirms phase varies across samples and is not just a label. | `+sixgr/+rf/PhaseNoiseModel.m`, `+sixgr/+link/applyWaveformImpairments.m`, `+sixgr/+link/applyWaveformTruthImpairments.m`; `testLLSExtendedPhysicalGuards`; pending mask-vs-measured oscillator profile calibration |
| CODEX-15 | Missing MMSE-IRC equalizer | Mostly fixed / focused pass | Receiver asks for IRC and falls back to MMSE only when covariance is unavailable; the focused post-eq test now proves covariance-backed IRC stays on the IRC detector/SINR path and exports covariance usage. | `+sixgr/+phy/+dl/PDSCH_Rx.m`, `+sixgr/+phy/+ul/PUSCH_Rx.m`, `+sixgr/+phy/+rx/mimoDetect.m`, `+sixgr/+phy/+rx/computePostEqSINR.m`; `testPostEqSINR`; pending multi-user interference covariance distribution audit |
| CODEX-16 | SRS does not cover full bandwidth | Mostly fixed / focused pass | SRS execution now exports actual occupied PRB count, carrier PRB count, bandwidth fraction, frequency start/end, and coverage status from `nrSRSIndices`; focused UL validation requires finite bandwidth evidence when SRS executes. | `+sixgr/+link/runSRSChannelEstimation.m`, `+sixgr/+phy/+ul/SRS_Tx.m`; `testLLS_UL`; pending scenario-specific full-bandwidth SRS allocation audit |
| CODEX-17 | 6G band pathloss support gap | Focused pass | Channel compliance tests pass for strict/approximate pathloss labeling and FR3/gap closed-form behavior. | `testChannelComplianceTruth`; pending broader 7-24.25 GHz scenario sweep |
| CODEX-18 | DFT-s-OFDM / pi/2-BPSK UL coverage | Mostly fixed / focused pass | PUSCH TX/RX now enforce transform-precoding ownership for pi/2-BPSK/DFT-s-OFDM paths and focused validation confirms a pi/2-BPSK PUSCH waveform materializes with native `nrPUSCH` transform precoding even when the input config initially omits it. | `+sixgr/+phy/+ul/PUSCH_Tx.m`, `+sixgr/+phy/+ul/PUSCH_Rx.m`, `+sixgr/+phy/+grid/allocREsPUSCH.m`, `tests/testLLSExtendedPhysicalGuards.m`; `testLLSExtendedPhysicalGuards`; pending multi-layer DFT-s candidate stress coverage |
| CODEX-19 | OFDM windowing / ACLR compliance | Mostly fixed / focused pass | OFDM windowing is resolved from explicit sample/percent config and passed into both DL and UL `nrOFDMModulate` paths; focused validation confirms PDSCH and PUSCH TX expose nonzero applied windowing evidence. | `+sixgr/+phy/+waveform/resolveOFDMWindowing.m`, `+sixgr/+phy/+dl/PDSCH_Tx.m`, `+sixgr/+phy/+ul/PUSCH_Tx.m`; `testLLSExtendedPhysicalGuards`; pending ACLR spectral-mask validation |
| CODEX-20 | KPI summary incomplete / missing health flags | Mostly fixed / focused pass | Added `reports/csv/kpi_health_flags.csv` as a first-class reporting-integrity artifact. It audits runtime row count, finite values, unavailable/diagnostic/proxy/fallback/bootstrap provenance, blocker reason, and recommended action for key DL/UL MCS, CQI, SINR, goodput, PUCCH, PUSCH, SRS, and PDCCH KPIs without fabricating missing KPI values. | `+sixgr/+truth/exportLLSOutputCoverageArtifacts.m`, `apps/lls_web_dashboard.py`; `testLLSOutputCoverageArtifacts`, `tests/test_lls_browser_output_coverage.py`; pending live-run KPI health review |
| CODEX-21 | SVG/PNG plots empty or misleading | Mostly fixed / focused pass | The old dormant measured-SINR-binning path for BLER/throughput-vs-SNR plots was removed, so SNR curves cannot be reconstructed from measured-quality bins if controlled sweep rows are absent. Plot manifest, render-status, data-quality, and browser plot payload tests now pass; live visual inspection remains for the next full WebGUI run. | `+sixgr/+truth/exportLLSReportingBundle.m`, `+sixgr/+truth/buildLLSReportingProvenanceTables.m`, `apps/lls_web_dashboard.py`; `testRepoProxyFileStateIntegrity`, `testLLSPlotDataValidation`, `tests/test_lls_browser_plot_browser.py`; pending live WebGUI plot audit |
| CODEX-22 | CDL-C NormalizePathGains power tracking | Focused pass | Config/channel focused tests pass; no current regression in strict profile handling. | `testConfig`, `testLLS_ReferencePoints`; pending absolute-power CDL-C long-run audit |
| CODEX-23 | MAC timing advance loop not closed | Mostly fixed / focused pass | Root cause was that PRACH success carried `TimingAdvance_samples/us`, but coupled runtime only updated access state and did not persist or refresh UE time-alignment state. PRACH now creates the TA baseline, SRS/UL receiver timing estimates refresh it, mobility recomputes propagation drift from UE/gNB geometry, measurement-only mode flags required TA updates without applying fake commands, geometry-predictive mode applies only a labeled propagation-derived delta, and control-state exports carry source/status/drift fields. Full 3GPP MAC CE TA command encoding/timer conformance remains a deeper follow-up. | `+sixgr/+truth/CoupledTruthRuntime.m`, `+sixgr/+lls6g/buildInternalConfig.m`, `simulator/configs/schema/scenario_parameter_catalog_extension_01.yaml`, `tests/testLLSControlAccessGating.m`; `testLLSControlAccessGating`, `testLLSControlArtifactExports`, `test6GScenarioConfigValidation`; pending MAC CE/timer conformance stress |
| CODEX-24 | Real controlled SNR sweep engine missing | Mostly fixed / verified | `runWaveformLinkBundle` has primary raw-trial sweep and fixed-reference sweep paths; receiver-noise/coupled-truth modes explicitly skip legacy configured-SNR sweeps. | `+sixgr/+truth/runWaveformLinkBundle.m`; `testLLSSweepTrialBudget` |
| CODEX-25 | 4x4 rank claim vs rank-1 runtime reality | Mostly fixed / focused pass | Multi-user 4x4 beamformed smoke now completes and verifies configured two-layer waveform execution for DL and UL. | `test6GLLSMultiUserBeamforming`; pending larger rank-adaptation run |
| CODEX-26 | CQI table lineage mismatch | Mostly fixed / focused pass | MCS helper resolves against configured CQI/MCS tables, and scheduler AMC now preserves measured-CQI vs bootstrap provenance through grant plan, raw trials, canonical exports, and CQI analytics. CQI table/grant sizing, closed-loop AMC, and CQI export diagnostics pass; complete YAML-to-runtime lineage audit remains for full WebGUI runs. | `resolveMCSIndexFromProfile.m`, `SchedulerBase.m`, `runDLPDSCHThroughput.m`, `runULPUSCHThroughput.m`, `exportLLSOutputCoverageArtifacts.m`; `testNRCQITableAndGrantSizing`, `testSchedulerAMCNoFixedMCSFallback`, `testLLSClosedLoopLinkAdaptation`, `testLLSCQIAnalyticsExportTruth` |
| CODEX-27 | UL CQI saturated at 15 | Mostly fixed / focused pass | Root cause was partly diagnostic and partly receiver dynamic range: UL CQI saturation was visible only after export, while post-eq SINR could become numerically extreme when effective noise variance collapsed. The receiver now applies the configured max-trusted SINR ceiling to scheduler-facing post-eq SINR in both DL and UL, preserves raw SINR metadata, marks `OK_dynamic_range_limited`, and MCS/TBS analytics expose CQI saturation diagnostics so all-CQI15 windows are auditable instead of silently contaminating charts. A live UL entropy audit remains for the next WebGUI run. | `+sixgr/+phy/+rx/computePostEqSINR.m`, `+sixgr/+phy/+ul/PUSCH_Rx.m`, `+sixgr/+phy/+dl/PDSCH_Rx.m`, `+sixgr/+phy/+ul/measureULLinkState.m`, `+sixgr/+truth/exportLLSOutputCoverageArtifacts.m`; `testPostEqSINR`, `testLLSExtendedPhysicalGuards`, `testConfig`, `testLLS_DL`, `testLLS_UL`, `testLLS_ReferencePoints`, `testLLSSINRFieldTruth`, `testLLSCQIAnalyticsExportTruth`; pending live UL CQI entropy/WebGUI run |
| CODEX-28 | EVM proxy as decoder truth | Fixed in this pass | Finite EVM-proxy SINR is quarantined to diagnostic metadata; primary decoder-truth SINR remains unavailable unless real decoder evidence exists. | `deriveDecoderTruthProxySINR.m`, `runWaveformLinkBundle.m`, `CoupledTruthRuntime.m`, `exportSystemLevelCanonicalArtifacts.m`; `testLLSSINRRuntimeArtifacts`, `testSystemLevelWaveformReplayEvidence` |
| CODEX-29 | PDCCH grant gate not blind decode | Mostly fixed / focused pass | PDCCH smoke and control coverage tests pass; candidate-level blind-search trace still needs large-run audit. | `test6GScenarioRunner`, `testTruthValidationControlCoverage` |
| CODEX-30 | Proxy/heuristic in link adaptation primary path | Mostly fixed / focused pass | Approximate MCS fallback was removed from key AMC/scheduler paths and strict no-proxy/no-fallback focused tests pass. Remaining proxy wording is restricted to explicit diagnostic, browser legacy-selection, documentation, or strict-fail guard contexts rather than primary truth LA rows. | `computeLinkAdaptationDecision.m`, `amcFromCQI.m`; `testRepoProxyFileStateIntegrity`, `testNoSyntheticBLERFallbacks`, `testStrictProxyGuards`, `testStrictMode_NoFallbackAnywhere` |
| CODEX-31 | `approxMCSIndex` in strict PHY paths | Fixed in this pass | Strict paths now call table-driven `resolveMCSIndexFromProfile`; old method is compatibility shim only. | `SchedulerBase.m`, `SystemLevelRunner.m`, `resolveMCSIndexFromProfile.m`; focused semantic smoke |
| CODEX-32 | DB/browser roundtrip consistency | Partial / improved | Canonical raw trial CSV schema preservation fixed for receiver SINR and precoder matrix dimensions, output coverage now includes implementation/health/persistence/API tables, and the browser payload renders KPI health and coverage artifacts from persisted CSV rows. Full MySQL-backed live run/browser route audit remains. | `pruneStructurallyBlankTableColumns.m`, `+sixgr/+truth/exportLLSOutputCoverageArtifacts.m`, `apps/lls_web_dashboard.py`; `testSystemLevelWaveformReplayEvidence`, `testLLSOutputCoverageArtifacts`, `tests/test_lls_browser_output_coverage.py`; pending live DB/browser audit |
| CODEX-33 | BLER-vs-SNR plot from bins instead of real sweep | Fixed in this pass | Reporting bundle no longer substitutes measured-quality bins into `bler_vs_snr`/`throughput_vs_snr` plots; the unused measured-quality binning helper was removed and the static guard now blocks reintroducing that shortcut. These plots render only from sweep rows or become placeholders. | `+sixgr/+truth/exportLLSReportingBundle.m`, `testRepoProxyFileStateIntegrity`, `testLLSSweepTrialBudget` |
| CODEX-34 | Per-layer SINR scalar not vector | Fixed in this pass | DL and UL receiver paths export `PostEqSINRPerLayer_dB`, and the multi-user 4T/2-layer regression now fails unless every two-layer row contains two finite per-layer SINR values. Runtime evidence from `results/lls/lls_multiuser_smoke/smoke/air_interface/csv/*_trials.csv` includes vector tokens such as `4.36009|-3.74576` and `4.34832|0.61295`. | `+sixgr/+phy/+dl/PDSCH_Rx.m`, `+sixgr/+phy/+ul/PUSCH_Rx.m`, `+sixgr/+link/runDLPDSCHThroughput.m`, `+sixgr/+link/runULPUSCHThroughput.m`, `tests/test6GLLSMultiUserBeamforming.m`; `test6GLLSMultiUserBeamforming` |
| CODEX-35 | PRACH restricted-set N_CS validation | Focused pass | PRACH LLS and control coverage tests pass, including prior PRACH format issue path. | `testPRACHLLS`, `testTruthValidationControlCoverage`; pending high-speed restricted-set stress coverage |
| CODEX-36 | Statistical confidence intervals for BLER/BER | Mostly fixed / verified | Sweep summaries export Wilson-style CI fields and the focused sweep-budget test validates CI bounds on the primary sweep. The broader report-bundle test is still too slow for this pass and is not counted as passed evidence here. | `runWaveformLinkBundle.m`; `testLLSSweepTrialBudget`; pending split/optimized `testLLSReportBundle` rerun |
| CODEX-37 | Strict conformance gate ResultOk | Fixed for prior focused failure | `test6GValidationRules` and `test6GScenarioRunner` now pass; truth contract reevaluates after final artifact exports. | Focused validation on 2026-06-11 |
| CODEX-38 | Missing implementation register | Fixed in this pass | `exportLLSOutputCoverageArtifacts` now emits `reports/csv/lls_implementation_register.csv` from the same runtime evidence that drives the coverage registry, completeness, API, persistence, and unavailable audits. Rows only allow conformance claims when implemented status has runtime/meta evidence plus persistence/API/export/UI gates; partial/unavailable/blocked rows stay non-claimable with blocker and next-action fields. | `+sixgr/+truth/exportLLSOutputCoverageArtifacts.m`, `apps/lls_web_dashboard.py`; `testLLSOutputCoverageArtifacts`, `tests/test_lls_browser_output_coverage.py` |
| CODEX-39 | Config active surface all inherited | Mostly fixed / focused pass | Config ownership exports already publish browser/scenario/internal/applied/measured status columns, runtime value-source dictionary, browser surface matrix, and status summary. Focused validation passes, so the old “everything looks inherited” audit failure is not reproduced; live WebGUI confirmation remains after the full run. | `+sixgr/+truth/exportLLSConfigOwnershipArtifacts.m`, `+sixgr/+config/buildParameterBindingMatrix.m`; `testLLSConfigOwnershipAuditExports` |
| CODEX-40 | Frontend truth badges / failed-run banner | Mostly fixed / focused pass | Browser live/status paths expose truth contract summary, inactive proxy proof, terminal promotion from truth-gated artifacts, output coverage cards, and the new implementation-register panel. Focused frontend tests pass; live intranet browser confirmation remains for the next full WebGUI run. | `apps/lls_web_dashboard.py`; `tests/test_lls_browser_truth_integrity.py`, `tests/test_lls_browser_terminal_promotion.py`, `tests/test_lls_browser_product_frontend.py`, `tests/test_lls_browser_output_coverage.py` |

Focused validations passed in this pass:

```matlab
setup6GRSimToolkit('Verbose',false); testRFInterferenceReconciliationArtifacts;
setup6GRSimToolkit('Verbose',false); testSharedDataPhysicalQueue('TDD');
setup6GRSimToolkit('Verbose',false); testSharedPUSCHChannelArtifacts;
setup6GRSimToolkit('Verbose',false); testNoiseDomainEvidenceContract; testFixedLinkDerivedReconciliationWiring;
setup6GRSimToolkit('Verbose',false); testPhase7CampaignEvidenceGates; testPhase7GateApplicability; testLLSProductionQualificationReduction;
setup6GRSimToolkit('Verbose',false); testLLSSINRRuntimeArtifacts;
setup6GRSimToolkit('Verbose',false); testCSIRuntimeExecution; testLLSExtendedPhysicalGuards; testSystemLevelWaveformReplayEvidence;
setup6GRSimToolkit('Verbose',false); test6GValidationRules; test6GScenarioRunner;
setup6GRSimToolkit('Verbose',false); test6GScenarioConfigValidation;
setup6GRSimToolkit('Verbose',false); testRepoProxyFileStateIntegrity; testLLSSweepTrialBudget;
setup6GRSimToolkit('Verbose',false); testLLSClosedLoopLinkAdaptation; testLLSCoupledTruthHARQRoundTrip;
setup6GRSimToolkit('Verbose',false); testLinkAdaptationDomainTruth; testLLSStatefulLinkAdaptationState; testLLSLinkAdaptationRadisysMCSLoop;
setup6GRSimToolkit('Verbose',false); testLLSEffectiveSINRBLERLUTSelection;
setup6GRSimToolkit('Verbose',false); testLLSHARQExercise;
setup6GRSimToolkit('Verbose',false); test6GLLSMultiUserBeamforming;
setup6GRSimToolkit('Verbose',false); testPRACHLLS; testTruthValidationControlCoverage;
setup6GRSimToolkit('Verbose',false); testLLSControlAccessGating; testLLSControlArtifactExports;
setup6GRSimToolkit('Verbose',false); testConfig; testLLS_DL; testLLS_UL; testLLS_ReferencePoints; testChannelComplianceTruth;
setup6GRSimToolkit('Verbose',false); testLLSOutputCoverageArtifacts;
setup6GRSimToolkit('Verbose',false); testLLSCQIAnalyticsExportTruth;
setup6GRSimToolkit('Verbose',false); testLLSExtendedPhysicalGuards;
setup6GRSimToolkit('Verbose',false); testLLSConfigOwnershipAuditExports;
```

```powershell
python tests\test_lls_browser_output_coverage.py
python tests\test_lls_browser_truth_integrity.py
python tests\test_lls_browser_terminal_promotion.py
python tests\test_lls_browser_product_frontend.py
python tests\test_lls_browser_canonical_artifact_selection.py
python tests\test_lls_contract_materialization.py
setup6GRSimToolkit('Verbose',false); testNoSyntheticBLERFallbacks; testStrictProxyGuards; testStrictMode_NoFallbackAnywhere;
```
