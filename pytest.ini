[pytest]
testpaths = tests
pythonpath = .
norecursedirs =
    results
    outputs
    artifacts
    node_modules
    .git
    .pytest_cache
    qualification_evidence
    canonical_runtime
    .webgui-venv
markers =
    webgui_unit: isolated canonical WebGUI unit and contract tests
    webgui_e2e: Playwright test against an externally managed WebGUI
