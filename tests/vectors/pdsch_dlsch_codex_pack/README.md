# 6GR PDSCH/DL-SCH Codex implementation pack


Pack scope:

- 12 source-specific PDSCH/DL-SCH implementation findings;
- 380 deterministic input rows and 518 bounded expected rows;
- 15 required production CSVs and 9 required production PNGs;
- dynamic DCI, downlink SPS activation, and isolated calibration assignment profiles;
- independent bounded vectors for scrambling, QAM, layer mapping, TBS/base-graph, TB CRC, DM-RS positions/ports, PT-RS conditions, reserved-RE unions, PRG behavior, and HARQ transitions.

Primary file:

- `CODEX_PROMPT_02_PDSCH_DLSCH.md` — implementation-grade Codex prompt.

Technical finding maps:

- `pdsch_dlsch_12_findings.csv` — the 12 PDSCH/DL-SCH findings, exact fixes, and acceptance tests.
- `current_pdsch_static_audit.csv` — source-visible shortcut checks against the uploaded simulator.

Executable vectors and expected outputs:

- generated by `generate_pdsch_independent_vectors.py`;
- protected by `independent_vector_manifest.json`;
- verified by `verify_pdsch_vector_pack.py`.

Output contracts and verifier:

- `desired_pdsch_csv_contract.csv`;
- `desired_pdsch_image_contract.csv`;
- `verify_pdsch_artifacts.py`.

Current limited execution evidence:

- `LIMITED_TEST_EXECUTION_REPORT.md`;
- `limited_pdsch_test_results.csv`;
- `limited_test_summary.json`;
- `existing_pdsch_artifact_presence_audit.csv`;
- `existing_pdsch_image_integrity_audit.csv`;
- `existing_pdsch_csv_inventory.csv`.

Recommended use:

1. copy this directory to `tests/vectors/pdsch/` or a repository audit folder;
2. paste the complete Codex prompt while Codex is opened at the repository root;
3. require Codex to edit and execute, not return a plan;
4. accept `COMPLETE` only after MATLAB tests and both Python verifiers pass.
