# 6GR WebGUI security, operations, and dashboard Codex pack

This pack implements phase 16 for the simulator. It contains a production Codex prompt, 12 findings, current-source evidence, RBAC and route matrices, deployment/security policies, a catalog-driven YAML/UI mapping, a normalized registry of all prior technical-pack artifacts, DUT-specific visualization design, security/config/quota/gallery test vectors, a decommission plan, and visual archetypes supplied by the user.

## Counts

- 12 findings
- 2158 unique catalog parameter paths
- 1197 unique artifact contracts
- 88 DUT/domain visualization definitions
- 1196 artifact-gallery test cases
- 49 mandatory test-plan rows
- 8 visual-reference images

## Use

Copy this directory into `tests/vectors/webgui/` or `audit/webgui_security_operations/` as instructed by the prompt. Give Codex the complete contents of `CODEX_PROMPT_15_WEBGUI_SECURITY_OPERATIONS_ARCHITECTURE_WITH_DASHBOARD.md`.

The visual references are design-only and must never be treated as simulator output.
