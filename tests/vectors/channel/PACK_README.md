# Channel/Geometry Codex Pack

1. Copy this directory to `tests/vectors/channel/` in the simulator repository.
2. Open Codex at the repository root.
3. Paste the full contents of `CODEX_PROMPT_10_CHANNEL_GEOMETRY_MOBILITY_INTERFERENCE_WITH_IMPACT_ANALYSIS.md`.
4. Require Codex to edit the production MATLAB source and execute every required command.
5. Do not accept plan-only output, unexecuted MATLAB, configured/reconstructed runtime evidence, or placeholder figures.
6. Accept completion only when all base/impact artifacts exist and both Python artifact verifiers return exit code 0.
